<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ice.dao.ModuleMapper">
    <select id="selectModule" resultType="java.util.HashMap">
       SELECT t.sid,t.mName FROM test_module t
    </select>
</mapper>